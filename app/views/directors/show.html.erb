<% director = Director.find_by(id: params["id"]) %>

<h1><%= director.name %></h1>

<p>Movies by this director:</p>

<ul>
  <% director.movies.each do |movie| %>
    <li><%= link_to movie.title, "/movies/#{movie.id}" %></li>
  <% end %>
</ul>
